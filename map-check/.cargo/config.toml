[alias]
build-electron = "build --profile electron"
test-release = "test --release"

# O build portable usado pelo Electron nao define target-cpu=native.
# Para um build local otimizado para esta maquina, use:
#   $env:RUSTFLAGS="-C target-cpu=native"; cargo build --release
